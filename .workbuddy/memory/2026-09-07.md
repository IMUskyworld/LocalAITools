# 2026-09-07

## 演示方案调研（暑期开发答辩 / 大创演示）

用户指定演示 PPT 存放目录：`C:\暑期开发ppt`（已存在，当时为空）。
需要给出 Windows 端 + Android 端的演示功能与场景建议。

### 核实结果：实际可演示能力（重要，与开发文档严重不符）

**Windows 端 LocalMind（均可演示）**
- 在线对话（DeepSeek deepseek-chat，key 已于 9-06 重新注入并端到端验证通过）
- **离线对话可用**：Ollama 已安装，且 `C:\Users\world\.ollama\models` 中已有
  `qwen2.5:7b`（4.4 GB）。与 `agent_server.py:578` 离线默认模型名完全一致。
  ⚠️ Ollama 默认**未运行**，演示前需先 `ollama serve`。
- Agent 7 工具全部可用：write_file / read_file / list_dir / move_file / open_app /
  read_clipboard / create_doc
- 文档生成 4 类：PPT / Word / Excel / PDF（make_doc.exe，已随安装包分发）
- 思考轨迹可视化（规划→执行→完成）、多会话、深浅色主题

**Android 端 LocalFile（可演示）**
- 只有 2 个页面：`Chat` + `Settings`（`LocalFileApp.kt` NavHost 仅注册这两个路由）
- **文件 AI 处理入口 = 聊天页附件**：`ChatViewModel.attachFile(uri)` 调用
  `fileProcessor.getFileInfo/readFileContent`，把内容拼进提示词
  "用户上传了文件「X」，内容如下，请基于文件内容回答用户的问题"。
  → 摘要/翻译/问答**能做**，但没有独立的文件处理页（README 主打的 P-A2 页面不存在）。
- 文档生成：聊天中经 `generate_doc` function calling 生成后分享

**明确不可演示（写了会翻车）**
- 手机远程控制电脑、扫码配对、RelayCloud 云端中继、new-api 网关、
  P2P/WebRTC、桌面截图回传 —— 代码已移除，仅存在于开发文档
- Android 独立「文件处理页」（P-A2）无 UI
- `ui/auth/AuthScreen.kt`（账号登录注册）未被任何地方引用，是死代码

### 给出的建议要点
- 开场用「一句话生成 PPT」自举演示（用自己的 create_doc 生成自己的答辩 PPT）
- 断网切离线作为差异化亮点（隐私本地处理的最强证据）
- Agent 真实操作电脑（整理桌面 / 总结剪贴板）视觉冲击强
- 提醒：勿照 `开发计划/README.md` 讲远程控制等功能
