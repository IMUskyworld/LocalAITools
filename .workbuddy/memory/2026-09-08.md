# 2026-09-08

## PPT 交付

按用户给的素材目录（`C:\暑期开发ppt\LocalMind素材` 26 个 + `LocaFile素材` 14 个）+ 项目报名描述，
做了一份 15 页答辩 PPT：`C:\暑期开发ppt\PPT\暑期开发-LocalAITools答辩.pptx`（4.5 MB）。

### 设计
- 配色：Ocean Gradient（深海蓝 065A82 / 青绿 1C7293 / 午夜 21295C），橙色 F59E0B 作离线/亮点强调
- 字体：Cambria 标题 + Calibri 正文
- 结构：深色封面 + 13 页浅色内容 + 深色封底，每页固定页脚（项目名 + 页码）

### 15 页
1. 封面（深色）
2. 目录（6 章节卡片）
3. 项目定位（报名表原文 + Agent/在线+离线/真干活 三个价值标签）
4. 三个核心价值（隐私本地 / 断网可用 / 真能干活）
5. 双端产品矩阵（LocalMind + LocalFile，中间 +）
6. LocalMind 主界面（截图 + 6 快捷指令说明）
7. Agent 思考链（截图 + 与聊天机器人的对比）
8. Agent 实战：贪吃蛇 HTML（write_file + open_app）
9. 一句话生成文档（4 类：PPT/Word/Excel/PDF）
10. 双模设计（离线对话截图 + 模型管理页）
11. LocalFile 主界面（截图 + 移动场景取舍）
12. 上传文件 → AI 处理（上传 → 提取要点 + 摘要/翻译/问答/重写）
13. 移动端文档生成（手写 Agent 循环 + Android 系统分享）
14. 技术栈与开源（GitHub / MIT / 70 MB+80 MB）
15. THANK YOU（已实现 / 下一步 / 联系方式）

### QA
- 文本 QA：通过（python-pptx 提取 15 页内容正确，无占位符）
- 视觉 QA：跳过（本机无 LibreOffice，soffice.py 在 Windows 上 socket.AF_UNIX 报错，
  临时下载几百 MB LibreOffice 不划算；待用户用 PowerPoint 打开确认效果）

### 关键风险控制
- 严格基于实际代码 + 真实素材，未提已删除的远程控制/扫码配对/云端中继/P2P 等功能
- 真实演示能力清单（offline Ollama qwen2.5:7b 已装 4.4 GB 等）与素材完全对应

### 可复用坑
- pptxgenjs 装在 `C:\code\LocalAITools-main\node_modules`，脚本若放在外部目录（如 Temp）
  需 `export NODE_PATH="C:/code/LocalAITools-main/node_modules"` 才能 require 到
- "LocaFile素材" 是用户拼写（少个 l），路径引用照此写
- markitdown 在 Windows 上卡死（pip install 超时），改用 python-pptx 轻量方案做文本 QA

## PPT 图片增强版（用户要求「多塞点图」）

重做为 **19 页 / 43 张图**（原 15 页 / 12 张），11.5 MB，几乎用尽素材。
- 新增页：更多界面细节(3 图)、桌面真实产物证据(4 图)、离线全流程(3 图)、
  LocalFile 界面(5 图)、LocalFile 结果预览+分享(4 图)
- **素材尺寸规律**：LocalMind 全部 1920x1200（比例 1.600），
  LocalFile 全部 1080x2412（比例 0.448）→ 按 `w / ratio` 算高度，排版不变形
- 长截图「提取上传的文件的要点.jpg」是 1080x6734，用 PIL 裁上部 1080x2600 存 temp 再用
- **坑**：文件名「未安装ollama时点击“离线”...」用的是**中文弯引号** U+201C/201D，
  脚本里误写成英文直引号 `"` 会 ENOENT 找不到文件
- 未用素材：`展示离线模型可切换.mp4`（PPT 无法内嵌视频，建议答辩现场播）

## 改版：面向大一新生的宣传讲解版（23 页）

用户补充需求：本次是**面向大一新生讲解的宣传**，
① 开头科普「Agent 是什么、和平时用的大模型有什么不一样」
② 讲公式 `Model + Harness = Agent`
③ 结尾加「欢迎加入联盟人工智能组 ~」

**新增 4 页（15→19→23 页，图片仍 43 张）**
- P3 大模型科普：会接话 / 本质是概率 / **只说不做**（右侧画了「你问→大脑→它答，
  桌面还是乱的」流程图）
- P4 Agent 对比：左「大模型 只会说」vs 右「Agent 会真的做」，中间 VS 圆章；
  举例：问「怎么整理桌面」vs 说「帮我整理桌面」
- P5 公式页：`[Model] + [Harness] = [Agent]` 三色块（蓝+青=橙）+ 下方三解释卡；
  底部点明本项目 = DeepSeek/Qwen（Model）+ Pydantic AI（Harness）
- P22 招新页：橙条深色底，三卡（我们做什么 / 你能学到 / 怎么加入）

**其他调整**
- 页脚文案「暑期开发答辩」→「项目分享」；封面副标题加「面向大一新生」
- 目录改为 6 章：先搞懂大模型与 Agent / 项目定位 / LocalMind / LocalFile / 技术栈 / 加入我们
- `addFooter` 改为**自动递增计数器**（`let _pg = 1`），插入新页后无需手改 18 处页码

**招新页内容说明**：三卡文案基于项目真实技术栈（Python/Rust/Kotlin）编写，
未编造具体招新时间/QQ群等信息；若需补充请用户提供。